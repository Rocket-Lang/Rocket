#include <string>
#include <vector>

namespace rocket {

	namespace lexer {



		// This function tests if the sequence it gets is a known
		// special-character-sequence. The sequence can be more than
		// 2 characters long, but only the first 2 characters will be
		// analyzed.
		bool isKown2CharSequence(std::string sequence) {


		}

		// This function tests if the sequence it gets is a known
		// special-character-sequence. The sequence can be more than
		// 3 characters long, but only the first 3 characters will be
		// analyzed.
		bool isKown3CharSequence(std::string sequence) {


		}

	}

}
